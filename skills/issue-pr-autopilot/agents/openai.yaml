interface:
  display_name: "Issue PR Autopilot"
  short_description: "Issue から OpenSpec 駆動で PR 作成とレビュー反復まで自走する"
  default_prompt: "Use $issue-pr-autopilot to drive an OpenSpec propose→apply cycle in a sibling worktree, open a PR, and run the review loop to APPROVED or HANDOFF."
